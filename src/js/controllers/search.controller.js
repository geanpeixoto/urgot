/**
 * @namespace Controller
 */
(function() {
    'use strict';

    angular.module('urApplication')
        .controller('SearchController', SearchController);

    /**
     * @namespace RadiographController
     * @memberOf Controller
     * @desc Controlador das radiografias
     * @ngInject
     */
    function SearchController ($routeParams, dataService) {
        var that = this;

        constructor();

        function constructor() {
        }
    }
})();
